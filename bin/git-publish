#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
JAR="$SCRIPT_DIR/../lib/git-publish.jar"

if [ ! -f "$JAR" ]; then
  echo "[ERROR] JAR not found: $JAR" >&2
  exit 1
fi

exec java -jar "$JAR" "$@"
